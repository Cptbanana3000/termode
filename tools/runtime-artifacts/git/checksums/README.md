# Git Artifact Checksums

Place checksum files for verified Git artifact payloads here.

Required:

- one SHA-256 entry for every payload file
- checksum values must match `manifest.json`
- no absolute paths
- no parent-directory traversal

v0.50 includes no real checksum file because no Git artifact is bundled.
