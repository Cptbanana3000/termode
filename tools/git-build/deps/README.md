# Dependency Sources

Place reviewed source trees for required Git dependencies here. Record each
version, upstream URL, license, archive checksum, Android build options, and
resulting library checksum. Likely dependencies include zlib and, for later
HTTPS support, libcurl plus a reviewed TLS strategy.

No dependency source is bundled in v0.52.
