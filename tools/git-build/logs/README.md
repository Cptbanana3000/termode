# Git Build Logs

Store reproducibility logs here: host/toolchain versions, configure options,
compiler commands, source and dependency checksums, and test results. Logs do
not make a build trusted by themselves; they support review of the artifact.
