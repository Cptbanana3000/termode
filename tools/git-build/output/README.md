# Git Build Output

Host-side arm64-v8a build and staging output belongs here. Output is not an app
artifact and is not installable until it is copied through the preparation
flow, receives a complete trusted manifest, and passes checksum validation.

Large generated files should not be committed accidentally.
