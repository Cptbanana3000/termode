# Git Bash Build Logs - Termode v0.62

All build logs for the Git arm64-v8a compilation attempt under Git Bash are stored on the host at:

- `tools/git-build/logs/git-arm64-build.log`

These logs capture the standard output and standard error from the Makefile invocation running under the POSIX Bash environment.
